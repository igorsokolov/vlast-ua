<%= form_for(@event) do |f| %>
  <% if @event.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@event.errors.count, "error") %> prohibited this event from being saved:</h2>

      <ul>
      <% @event.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :event_date %><br>
    <%= f.text_field :event_date%>
  </div>

  <div class="field">
    <%= f.label :title %><br>
    <%= f.text_area :title %>
  </div>
  <div class="field">
    <%= f.label :description %><br>
    <%= f.text_area :description %>
  </div>
  <div class="field">
    <%= f.label :source_id %><br>
    <%= f.select :source_id, Entity.all.collect{|e| [e.name, e.id]} %>
  </div>
  <div class="field">
    <%= f.label :destination_id %><br>
    <%= f.select :destination_id, Entity.all.collect{|e| [e.name, e.id]} %>
  </div>
  <div class="field">
    <%= f.label :object_id %><br>
    <%= f.select :object_id, Entity.all.collect{|e| [e.name, e.id]} %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
